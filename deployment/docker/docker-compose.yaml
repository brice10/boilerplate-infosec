version: '3'

services:
  boilerplate-american-british-english-translator-api:
    build:
      context: ../..
      dockerfile: ./Dockerfile
      args:
        - PORT=${PORT}
    ports:
      - ${PORT}:${PORT}
    restart: unless-stopped